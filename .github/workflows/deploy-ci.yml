name: Django Voting-App CI & Deploy (basic)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install django pytest flake8
          fi

      - name: Run lint (flake8)
        run: |
          # assume your code base root
          flake8 .

      - name: Run Django tests
        run: |
          python manage.py test

  deploy:
    needs: build_and_test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to server
        run: |
          echo "Deploy step placeholder – add your deploy commands here"
          # e.g. heroku git:remote -a ${{ secrets.HEROKU_APP_NAME }}
          #       git push heroku main
